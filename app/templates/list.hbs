{{! LEFT }}
{{#unless this.eventCategoryDropdown}}
  <div class="uk-visible@m uk-width-1-4@m">
    <div class="uk-card uk-card-small uk-card-body">
      {{area-navigation this.model}}
    </div>
  </div>
{{/unless}}

{{! CENTER }}
<div id="eventList" class="{{this.centerWidth}} center-container uk-margin-bottom">
  {{outlet}}
</div>
