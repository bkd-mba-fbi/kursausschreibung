<input id="vss{{this.field.id}}" class="uk-input" type="email" name={{this.field.id}} autocomplete="email" required={{this.field.options.required}}
  autocomplete={{this.field.options.autocomplete}} disabled={{this.field.options.disabled}}>
